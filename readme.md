# gig-ui

An UI library. V language. Initially using raylib as the graphics backend.



## Usage

So far, Raylib is linked dinamically. The user would need to install the shared raylib library.
Later, will seek to link statically, to avoid this issue.
Or include any required dependency directly, as a file.